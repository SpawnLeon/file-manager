export  const EXIT_MESSAGE =  (username) => {
  return `Thank you for using File Manager, ${username}, goodbye!`
}

export  const GREAT_MESSAGE =  (username) => {
  return `Welcome to the File Manager, ${username}!`
}
